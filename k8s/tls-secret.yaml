apiVersion: v1
kind: Secret
metadata:
  name: wisecow-tls-secret
  namespace: wisecow
type: kubernetes.io/tls
data:
  # Base64-encoded TLS certificate and key
  # Replace these with actual values or use deploy.sh to auto-generate
  tls.crt: ""
  tls.key: ""
